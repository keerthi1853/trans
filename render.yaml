services:
  - type: web
    name: upi-trans
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run streamlit_transaction.py --server.port $PORT --server.address 0.0.0.0
    envVars:
      - key: SECRET_KEY
        generateValue: true
